<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable name="user" type="org.iflab.wecenterandroid.modal.Answer.RsmEntity.AnswerEntity.UserInfoEntity"/>
        <variable name="questionUser" type="org.iflab.wecenterandroid.modal.Question.RsmEntity.QuestionInfoEntity.UserInfoEntity"/>

        <variable
            name="question"
            type="org.iflab.wecenterandroid.viewmodal.QuestionViewModal"/>
    </data>
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="10dp">

        <ImageView
            android:id="@+id/iv_avatar"
            android:layout_width="45dp"
            android:layout_height="45dp"
            app:userInfoItemAvatar="@{user != null ?user.getAvatar_file():questionUser.getAvatar_file()}"
            />

        <TextView
            android:id="@+id/tv_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toRightOf="@+id/iv_avatar"
            android:layout_marginLeft="10dp"
            android:text="@{user != null ?user.getUser_name():questionUser.getUser_name()}"
            />

        <TextView
            android:id="@+id/tv_sign"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@+id/iv_avatar"
            android:layout_alignStart="@+id/tv_name"
            android:text="@{user != null ?user.getSignature():questionUser.getSignature()}"
            android:layout_alignRight="@+id/tv_name"
            android:layout_alignEnd="@+id/tv_name" />

    </RelativeLayout>
</layout>